name: Hello world

# trigger manually
on: push

jobs:
   build:
       runs-on: ubuntu-latest
       steps:
           - name: Build Step
             run: |
                echo " Hello , ${{vars.My_Name}}"
                echo " Hello, ${{secrets.Docker_Password}} "
 

